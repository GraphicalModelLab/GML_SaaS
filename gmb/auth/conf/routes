
+nocsrf
GET                 /index                    controllers.AuthController.index()
+nocsrf
GET                 /helloworld                    controllers.AuthController.helloworld()
+nocsrf
POST                /auth/:companyId/login                    controllers.AuthController.login(companyId:String)
+nocsrf
POST                /auth/:companyId/register                    controllers.AuthController.register(companyId:String)
+nocsrf
POST                /auth/:companyId/validation                    controllers.AuthController.validation(companyId:String)
+nocsrf
POST                /auth/:companyId/changeRole                    controllers.AuthController.changeRole(companyId:String)
+nocsrf
POST                /auth/:companyId/changePassword                    controllers.AuthController.changePassword(companyId:String)
+nocsrf
POST                /auth/:companyId/removeAccount                    controllers.AuthController.removeAccount(companyId:String)
+nocsrf
POST                /auth/:companyId/googleApps/authenticate                    controllers.AuthController.googleAppsAuthenticate(companyId:String)
+nocsrf
POST                /auth/:companyId/facebookApps/authenticate                    controllers.AuthController.facebookAppsAuthenticate(companyId:String)
+nocsrf
POST                /auth/:companyId/registerCompany                    controllers.AuthController.registerCompany(companyId:String)
+nocsrf
POST                /auth/:companyId/social/connect/status                    controllers.AuthController.getSocialConnectStatus(companyId:String)
+nocsrf
POST                /auth/:companyId/social/connect/facebook/disconnect                    controllers.AuthController.disconnectFacebookConnection(companyId:String)